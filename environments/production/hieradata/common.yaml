---
classes:
  -  foodity-common
  -  foodity-puppet

software:
  pv:
    ensure: 'latest'
  git:
   ensure: 'latest'
  python-pip: 
    ensure: 'latest'
  build-essential: 
    ensure: 'latest'
  checkinstall: 
    ensure: 'latest'
  lynx:
    ensure: 'latest'
  nmap:
    ensure: 'latest'
  dselect:
    ensure: 'latest'
  tree:
    ensure: 'latest'
  expect:
    ensure: 'latest'
  ruby1.9.1-dev:
    ensure: 'latest'
