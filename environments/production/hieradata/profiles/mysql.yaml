---
classes:
 - profiles::mysql::server

profiles::mysql::server::root_password: 'rewit555'

profiles::mysql::server:
  databases:
    'service':
      user: 'service'
      password: 'changeme'
      charset: 'utf8'
      host: 'localhost'
      ensure: 'present'

profiles::mysql::server:
  users:
  'claudio@localhost':
    ensure: 'present'
    password_hash: '*7ACE763ED393514FE0C162B93996ECD195FFC4F5'

